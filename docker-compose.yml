version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=postgresql://neondb_owner:npg_Ph0mzTf4iBkK@ep-polished-mud-aiyivle2.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require
      - PORT=5000
      - JWT_SECRET=taskly_super_secret_jwt_key_2026_freelancer_project
      - NODE_ENV=production

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    environment:
      - VITE_API_URL=http://localhost:5000/api
    depends_on:
      - backend
